&НаКлиентеПроцедура СписокВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)    // Находим документ РеализацияОпционов, связанный с выбранной заявкой    СсылкаНаДокументРеализации = НайтиДокументРеализации(ВыбраннаяСтрока);        Если СсылкаНаДокументРеализации <> Неопределено Тогда        // Открываем форму документа РеализацияОпционов        ОткрытьФормуДокумента(СсылкаНаДокументРеализации);    Иначе        Сообщить("Документ РеализацияОпционов для заявки " + ВыбраннаяСтрока.Номер + " не найден.", СтатусСообщения.Внимание);    КонецЕсли;КонецПроцедуры  Функция НайтиДокументРеализации(ВыбраннаяСтрока)     Запрос = Новый Запрос;    Запрос.Текст =         "ВЫБРАТЬ ПЕРВЫЕ 1         | ССЫЛКА         |ИЗ Документ.РеализацияОпционов        |ГДЕ НомерЗаявки = &НомерЗаявки"; // Предполагаем, что есть поле НомерЗаявки, связывающее документы        Запрос.УстановитьПараметр("НомерЗаявки", ВыбраннаяСтрока.Номер);    РезультатЗапроса = Запрос.Выполнить();        Если РезультатЗапроса.Пустой() Тогда        Возврат Неопределено; // Документ не найден    КонецЕсли;        // Извлекаем данные из результата запроса    Выборка = РезультатЗапроса.Выбрать();    Если Выборка.Следующий() Тогда        Возврат Выборка.ССЫЛКА; // Возвращаем ссылку на документ РеализацияОпционов    КонецЕсли;        Возврат Неопределено; // Если ничего не найденоКонецФункции&НаКлиентеПроцедура ОткрытьФормуДокумента(СсылкаНаДокумент)    // Создаем структуру с параметрами для открытия формы    ПараметрыФормы = Новый Структура;    ПараметрыФормы.Вставить("Ссылка", СсылкаНаДокумент);        // Открываем форму документа РеализацияОпционов с параметрами    ОткрытьФорму("Документ.РеализацияОпционов.Форма.ФормаДокумента", ПараметрыФормы);КонецПроцедуры